import {StyleSheet} from 'react-native';

const estilo = StyleSheet.create({
    fotoUsuario: {
        width: 40,
        height: 40,
        margin: 10,
        borderRadius: 20,
    },
    cabecalho: {
        flexDirection: 'row',
        alignItems: 'center',
    },
});

export default estilo;
